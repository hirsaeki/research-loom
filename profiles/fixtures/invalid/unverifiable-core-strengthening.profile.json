{
  "schema_version": "0.1.0",
  "profile_id": "fixture.unverifiable-strengthening",
  "profile_type": "research",
  "profile_version": "1.0.0",
  "core_compatibility": {
    "research_contract": ">=0.1.0 <0.2.0",
    "invariant_contract": ">=0.1.0 <0.2.0"
  },
  "constraints": [
    {
      "id": "FIX-BAD-INV-001",
      "path": "evidence.capture.display_label",
      "merge_strategy": "must_equal",
      "value": "exact",
      "rationale": "The path is unrelated to the registered exact-locator form."
    }
  ],
  "core_invariant_strengthenings": [
    {
      "invariant_id": "CORE-TRACE-001",
      "effect": "strengthen",
      "constraint_ids": ["FIX-BAD-INV-001"],
      "validator_binding": {
        "validator_id": "core.trace.evidence-source",
        "validator_version": "0.1.0",
        "form_id": "exact-locator"
      },
      "rationale": "Binding exists, but the referenced constraint does not match the approved form."
    }
  ]
}
