# Research Capability fixtures

Synthetic executable fixtures for PR 9. The chain is:

`Capability Descriptor -> bounded Context Pack -> Invocation -> Handoff`

The Context Pack binds the existing generic Project Config, Effective Profile Set, and Core Research Snapshot and carries Research Attention, project guards, and Effective Profile constraints without silent truncation. The generic invocation is `virtual`, so all evidentiary outputs are synthetic candidates.

The Handoff proves that next actions/methods remain proposals, Writer/Publication artifacts cannot be Research Evidence, and valid/partial/rejected validation never adopts output into authoritative Research State. It also supports a newly acquired source capture so later Desktop Research-style plugins are not restricted to pre-registered Project sources.
